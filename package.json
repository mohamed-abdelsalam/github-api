{
  "name": "github-api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "clean": "rm -rf node_modules && rm -rf dist && rm -rf coverage",
    "build": "npm run clean && npm install && ./node_modules/.bin/tsc",
    "test": "jest --coverage --no-cache"
  },
  "author": "salam",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
